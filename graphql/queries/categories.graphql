query Categories {
  categories {
    items {
      id
      name
      url_key
      children {
        id
        name
        url_key
        children {
          id
          name
          url_key
          meta_title
          meta_description
          description
        }
      }
    }
  }
}
